/**
 * Cœur métier — TypeScript pur, sans aucune dépendance UI.
 * Modèle de données, moteur de contraintes/effets, calcul de coût.
 * Tout le reste de l'application dépend de ce module ; lui ne dépend de rien d'autre.
 */

export * from "./model";
